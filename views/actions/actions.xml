<odoo>
    <record id="action_prescription_customer_prescription" model="ir.actions.act_window">
        <field name="name">Prescriptions</field>
        <field name="res_model">prescription.customer.prescription</field>
        <field name="view_mode">list,form</field>
        <field name="views" eval="[
        (ref('view_prescription_customer_prescription_list'), 'list'),
        (ref('view_prescription_customer_prescription_form'), 'form')
        ]"/>
        <!-- <field name="domain">[('company_id', 'in', allowed_company_ids)]</field> -->
    </record>
</odoo>